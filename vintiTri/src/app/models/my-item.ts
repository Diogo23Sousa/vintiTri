export class MyItem {
    type: string;
    model: string;
    color: string;
    size: string;
    price: string;

    constructor(type, model, color, size, price) {
        this.type = type;
        this.model = model;
        this.color = color;
        this.size = size;
        this.price = price;
        }
    }